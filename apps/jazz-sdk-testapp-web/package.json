{
  "name": "jazz-sdk-testapp-web",
  "version": "25.41.0",
  "description": "SaluteJazz SDK Demo Web App",
  "private": true,
  "scripts": {
    "start": "cross-env NODE_ENV=development webpack serve --mode development --hot only",
    "clean": "rm -rf dist",
    "lint": "npx tsc --noEmit"
  },
  "dependencies": {
    "jazz-sdk-testapp-universal": "*"
  }
}
